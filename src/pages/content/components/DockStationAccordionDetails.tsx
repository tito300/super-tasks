import {
  AccordionDetails,
  accordionDetailsClasses,
  collapseClasses,
  styled,
} from "@mui/material";

export const DockStationAccordionDetails = styled(AccordionDetails)(
  ({ theme }) => ({
    padding: 0,
  })
);
